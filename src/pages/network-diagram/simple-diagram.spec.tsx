import { render } from "react-dom";
import SimpleDiagram from "./simple-diagram";

describe("simple diagram", () => {
  // let baseElement, container, screen;
  // beforeEach(() => {
  //   ({ baseElement, container, ...screen } = render(<SimpleDiagram />));
  //   // expect(baseElement).toMatchSnapshot();
  // });

  // it("Matches snapshot", () => {
  //   // ({ baseElement, container, ...screen } = render(<SimpleDiagram />))

  //   expect(baseElement).toMatchSnapshot();
  // });
});
